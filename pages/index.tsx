import type { NextPage } from "next";
import { Loading } from "../components/Loading";

const Home: NextPage = () => {
  return (
    <div>
      <main>Content</main>
      <Loading />
    </div>
  );
};

export default Home;
